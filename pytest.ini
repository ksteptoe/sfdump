[pytest]
# Register custom markers
markers =
    live: marks tests that require real Salesforce connectivity

# Default: skip any live tests unless explicitly enabled
addopts = -m "not live"

# Optional: make output a little cleaner
testpaths = tests
python_files = test_*.py
